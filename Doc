docket pull centos -> goes on DockerHub and pull the centos image

docker run -d -t --name containerName nameOfTheImageToPull[:possibleTag]
	-p a:b specify the port. a is the host port, b is the container port
	
docker ps -> see the different dockers currenty running

docker exec -it containerName bash/sh -> goes into the specified docker
exit

docker stop containerName
docket start containerName
docker stats


see --rm
docker network create networkName
--network networkName
